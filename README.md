# Python_Task1
Developed a Flask-based web application with structured architecture using blueprints, controllers, services, and SQLAlchemy models. Implemented user authentication (register, login, logout), database schema versioning with Flask-Migrate/Alembic, and applied migrations to evolve the database safely.
